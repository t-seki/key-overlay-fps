<Window x:Class="KeyOverlayFPS.TestDynamicLayoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dynamic Layout Test" 
        Width="580" 
        Height="160"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="True"
        ResizeMode="NoResize"
        Background="Transparent">
    
    <!-- 動的に生成されるCanvas -->
    <Canvas x:Name="DynamicCanvas" Margin="8" Background="Transparent" />
    
</Window>